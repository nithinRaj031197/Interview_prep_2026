/**
 * Template for a new machine coding project.
 *
 * Steps:
 * 1. Copy this folder to src/projects/<yourProjectName>/
 * 2. Rename ProjectName.tsx / ProjectName.css to match your component
 * 3. Register in src/config/projects.ts
 */
import "./ProjectName.css";

const ProjectName = () => {
  return (
    <div className="project-name">
      <p>Replace with your machine coding implementation.</p>
    </div>
  );
};

export default ProjectName;
